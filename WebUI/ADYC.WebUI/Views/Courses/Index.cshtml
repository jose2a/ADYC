@model IEnumerable<ADYC.WebUI.Models.Course>

@{
    ViewBag.Title = "Courses";
}

<h2>Courses</h2>

<p>
    @if (TempData["Msg"] != null)
    {
        @TempData["Msg"]
    }
</p>

<div class="btn-toolbar">
    <div class="btn-group">
        <a href="@Url.Action("new")" class="btn btn-primary btn-sm"><i class="glyphicon glyphicon-plus"></i> Create Course</a>
    </div>
</div>

<table id="courses" class="table table-bordered table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(m => m.Id)
        </th>
        <th>
            @Html.DisplayNameFor(m => m.Name)
        </th>
        <th>
            Type
        </th>
        <th></th>
    </tr>

    @foreach (var course in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(m => course.Id)
            </td>
            <td>
                @Html.DisplayFor(m => course.Name)
            </td>
            <td>
                @Html.DisplayFor(m => course.CourseType.Name)
            </td>
            <td>
                <a href="@Url.Action("edit", new {course.Id})" class="btn btn-xs btn-primary">
                    <i class="glyphicon glyphicon-edit"></i> Edit
                </a>

                @if (!course.IsDeleted)
                {
                    <div class="btn-group btn-group-xs">
                        <button data-course-id="@course.Id" class="btn btn-danger js-remove" data-post="Are you sure you want to trash this course?"><i class="glyphicon glyphicon-remove"></i> Trash</button>
                        <a href="#" class="btn btn-danger dropdown-toggle" data-toggle="dropdown"><span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li>
                                <a href="@Url.Action("delete", new { course.Id })" data-course-id="@course.Id" class="js-delete" data-post="Are you sure you want to delete this course FOREVER?">
                                    <i class="glyphicon glyphicon-remove"></i> Delete
                                </a>
                            </li>
                        </ul>
                    </div>
                }
                else
                {
                    <a href="@Url.Action("restore", new { course.Id })" class="btn btn-xs btn-success" data-post="">
                        <i class="glyphicon glyphicon-plus-sign"></i> Restore
                    </a>
                }
            </td>
        </tr>
    }

</table>
@section scripts
{
<script>
    $(document).ready(function () {
        $("#courses").on("click", ".js-remove", function (e) {
            //e.preventDefaults();

            console.log(e);

            var button = $(this);

            console.log(button.attr("data-course-id"));

            bootbox.confirm("Are you sure you want to delete this course?", function (result) {
                if (result) {
                    console.log(result);
                    //$.ajax({
                    //    url: "/api/movies/" + button.attr("data-movie-id"),
                    //    method: "DELETE",
                    //    success: function () {
                    //        table.row(button.parents("tr")).remove().draw();

                    //        console.log("Success");
                    //    }
                    //});
                }
            });
        });

        $("#courses").on("click", ".js-delete", function (e) {
            e.preventDefault();

            console.log("Deleting the course");

            var button = $(this);

            console.log(button.attr("data-course-id"));

            bootbox.confirm("Are you sure you want to delete this course?", function (result) {
                if (result) {
                    console.log(result);
                    //$.ajax({
                    //    url: "/api/movies/" + button.attr("data-movie-id"),
                    //    method: "DELETE",
                    //    success: function () {
                    //        table.row(button.parents("tr")).remove().draw();

                    //        console.log("Success");
                    //    }
                    //});
                }
            });
        });

    });
</script>
}