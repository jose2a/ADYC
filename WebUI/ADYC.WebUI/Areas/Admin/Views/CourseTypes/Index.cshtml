@model IEnumerable<ADYC.API.ViewModels.CourseTypeDto>

@{
    var title = "Course Types";
    ViewBag.Title = title;
    ViewBag.PageHeader = title;

    ViewBag.BreadcrumbLinks = new Dictionary<string, string>
    {
        [title] = ""
    };

    ViewBag.ActiveBreadcrumb = title;
}

<div class="row">
    <div class="col-xs-12">
        <div class="box">
            <div class="box-header with-border">
                <div class="btn-toolbar">
                    <div class="btn-group">
                        <a href="@Url.Action("new")" class="btn btn-primary btn-sm"><i class="fa fa-plus"></i> Create Course Type</a>
                    </div>
                </div>
            </div>
            <div class="box-body">
                <table id="courseTypes" class="table table-bordered table-hover table-striped dataTable" role="grid">
                    <thead>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(m => m.Id)
                            </th>
                            <th>
                                @Html.DisplayNameFor(m => m.Name)
                            </th>
                            <th class="actions">Actions</th>
                        </tr>
                    </thead>
                    <tbody>

                        @foreach (var courseType in Model)
                        {
                            @Html.Partial("pv_CourseTypeRow", courseType)

                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section scripts
{
    <script>
        $(document).ready(function () {

            var tableHelper = TH$({
                table: "courseTypes",
                messageDiv: "#msg-div",
                addDeleteEvent: true
            });

            $('#courseTypes').DataTable();
        });
    </script>

    <script type="text/javascript" src="~/Scripts/adyc.table.utils.js"></script>
    <script type="text/javascript" src="https://cdn.datatables.net/1.10.19/js/jquery.dataTables.min.js"></script>
    <script type="text/javascript" src="https://cdn.datatables.net/1.10.19/js/dataTables.bootstrap.min.js"></script>
}